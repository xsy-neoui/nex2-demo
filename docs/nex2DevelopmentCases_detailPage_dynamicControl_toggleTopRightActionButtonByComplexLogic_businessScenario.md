# 业务场景
在标准系统中，详情页右上角默认显示所有操作按钮（如编辑、删除、导出等），无法根据用户身份和业务条件动态控制可见性。为提升页面安全性与操作准确性，本案例通过在详情页中引入条件判断机制，实现按钮的动态展示与隐藏，使页面交互逻辑更符合权限模型与业务要求。

以签约合同管理为例，企业内部不同岗位和部门对合同的操作权限存在差异。例如：

-   只有合同负责人具备编辑权限。
-   合同负责人仅可编辑金额不超过 500 万且状态为“草稿”的合同。
-   只有默认管理员可以删除合同。
