# 业务场景
在采购申请等存在主子表结构的业务表单中，子表通常包含“数量”、“金额”等字段。

若用户在填写过程中录入了数量为 0 的数据，可能导致业务审批或财务处理出现风险。同时，主表往往会设置一个“总预算金额”字段，用于控制整体预算或成本支出。

因此，在用户提交采购申请单时，需要自动校验以下内容：

-   每条子明细的数量字段必须大于 0。
-   所有子明细的金额合计不得超过主表中定义的总金额字段。
